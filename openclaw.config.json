{
  "channels": {
    "discord": {
      "token": "${DISCORD_TOKEN}",
      "dmPolicy": "open"
    }
  },
  "models": {
    "default": "gemini/gemini-2.0-flash"
  },
  "keys": {
    "google": "${GOOGLE_API_KEY}"
  }
}
